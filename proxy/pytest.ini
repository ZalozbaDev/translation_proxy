[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -ra -q -m "not live and not audio"
markers =
    live: requires the docker-compose stack running (skipped by default; run with `pytest -m live`)
    audio: requires AUDIO_FIXTURES_DIR pointing at a fixture manifest (skipped by default; run with `pytest -m audio`)
